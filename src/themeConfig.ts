import { ThemeConfig } from 'antd';

export const themeConfig: ThemeConfig = {
  components: {
    Typography: {
      titleMarginBottom: 0,
      titleMarginTop: 0,
    },
    Layout: {
      headerBg: '#F5F3ED',
    },
  },
};
